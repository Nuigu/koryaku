<!DOCTYPE html>
<html>
  <head>
    <title>Koryaku</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+JP:wght@300;400;700&display=swap" rel="stylesheet">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header>
      <div class="header-logo"><%= link_to "KORYAKU", root_path, class: "header-link" %></div>
      <div class="header-right">
        <% if user_signed_in? %>
          <div class="header-menu"><%= link_to current_user.name, "#", class: "header-link" %></div>
          <div class="header-menu"><%= link_to "ログアウト", destroy_user_session_path, data: {turbo_method: :delete}, class: "header-link" %></div>
        <% else %>
          <div class="header-menu"><%= link_to "ログイン", new_user_session_path, class: "header-link" %></div>
          <div class="header-menu"><%= link_to "新規登録", new_user_registration_path, class: "header-link" %></div>
        <% end %>
      </div>
    </header>
    <div class="main">
    <%= yield %>
    </div>
  </body>
</html>
